#!/usr/bin/env bash
i=0
while [ $i -lt 40 ]; do
  ((i++))
  <%= @docker_cmd %> ps | head -n 1 | grep ^CONTAINER > /dev/null 2>&1
  [ $? -eq 0 ] && break
  sleep 0.5
done
[ $i -eq 20 ] && exit 1
exit 0
